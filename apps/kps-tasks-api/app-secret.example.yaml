# Modele, sans valeur reelle -- voir postgres-secret.example.yaml pour l'explication.
apiVersion: v1
kind: Secret
metadata:
  name: kps-tasks-api-secret
  namespace: kps-tasks
type: Opaque
stringData:
  DATABASE_URL: "postgresql+psycopg://REPLACE_USER:REPLACE_PASSWORD@postgres:5432/kps_tasks_db"
